[package]
name = "skill-registry"
version = "0.1.0"
edition = "2021"

[dependencies]
httpdate = "1"
reqwest = { version = "0.13", default-features = false, features = ["blocking", "rustls"] }
serde_json = "1"
thiserror = "2"
