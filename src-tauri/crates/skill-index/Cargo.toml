[package]
name = "skill-index"
version = "0.1.0"
edition = "2021"

[dependencies]
rusqlite = { version = "0.31", features = ["bundled"] }
skill-core = { path = "../skill-core" }
skill-local = { path = "../skill-local" }
thiserror = "2"

[dev-dependencies]
tempfile = "3"
