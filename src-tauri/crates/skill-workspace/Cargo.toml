[package]
name = "skill-workspace"
version = "0.1.0"
edition = "2021"

[dependencies]
skill-core = { path = "../skill-core" }
skill-harness = { path = "../skill-harness" }
skill-local = { path = "../skill-local" }
thiserror = "2"
uuid = { version = "1", features = ["v4"] }

[dev-dependencies]
tempfile = "3"
